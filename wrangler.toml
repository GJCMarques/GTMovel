# Cloudflare Pages Configuration for GT Móvel
# This file tells Cloudflare how to deploy the static site + Functions

name = "gt-movel"
compatibility_date = "2026-01-15"

# Pages configuration
pages_build_output_dir = "."

# Assets configuration - serve everything as static files
[assets]
directory = "."
